# SwiftCart - E-Commerce Website

একটি আধুনিক এবং প্রতিক্রিয়াশীল ই-কমার্স ওয়েবসাইট যা HTML, CSS এবং ভ্যানিলা JavaScript দিয়ে তৈরি।

## বৈশিষ্ট্য

- ✨ প্রতিক্রিয়াশীল ডিজাইন (মোবাইল, ট্যাবলেট, ডেস্কটপ)
- 🛍️ পণ্য ফিল্টারিং এবং ক্যাটাগরি বাছাই
- 🛒 শপিং কার্ট কার্যকারিতা স্থানীয়ভাবে সংরক্ষণ সহ
- 📱 রিয়েল-টাইম API ডেটা (Fake Store API)
- 🎨 মসৃণ অ্যানিমেশন এবং ট্রানজিশন
- 📦 পণ্য বিস্তারিত মডাল

---

# জাভাস্ক্রিপ্ট প্রশ্ন এবং উত্তর

## ১. `null` এবং `undefined` এর মধ্যে পার্থক্য কি?

**উত্তর:**

`null` এবং `undefined` উভয়ই জাভাস্ক্রিপ্টে খালি মান প্রকাশ করে, কিন্তু তাদের মধ্যে গুরুত্বপূর্ণ পার্থক্য রয়েছে:

### `undefined`:

- এটি একটি প্রাথমিক ডেটা টাইপ যা স্বয়ংক্রিয়ভাবে নির্ধারিত হয়
- যখন কোনো ভেরিয়েবল ঘোষণা করা হয় কিন্তু কোনো মান দেওয়া হয় না তখন এটি ঘটে
- ফাংশন যখন কোনো মান রিটার্ন করে না তখন `undefined` রিটার্ন করে
- ফাংশন প্যারামিটার যখন সরবরাহ করা হয় না তখন `undefined` হয়

```javascript
let x;
console.log(x); // undefined

function test() {}
console.log(test()); // undefined
```

### `null`:

- এটি একটি নির্দিষ্ট মান যা প্রোগ্রামার দ্বারা স্পষ্টভাবে নির্ধারিত হয়
- এটি "কোনো মান নেই" বা "খালি মান" প্রকাশ করার উদ্দেশ্যমূলক উপায়
- এটি সবসময় বিকাশকারীর দ্বারা সেট করা হয়, কখনও স্বয়ংক্রিয়ভাবে নয়

```javascript
let y = null;
console.log(y); // null
```

### পার্থক্য বোঝার টেবিল:

| বিষয়    | `undefined`       | `null`       |
| -------- | ----------------- | ------------ |
| ধরণ      | অগ্রাধিকার        | objective    |
| নির্ধারণ | স্বয়ংক্রিয়      | স্পষ্ট       |
| অর্থ     | মান সেট করা হয়নি | কোনো মান নেই |

---

## ২. জাভাস্ক্রিপ্টে `map()` ফাংশনের ব্যবহার কি? এটি `forEach()` থেকে কীভাবে ভিন্ন?

**উত্তর:**

### `map()` ফাংশন:

`map()` একটি array মেথড যা প্রতিটি উপাদানে একটি কলব্যাক ফাংশন প্রয়োগ করে এবং **নতুন array রিটার্ন করে**।

```javascript
const numbers = [1, 2, 3, 4];
const doubled = numbers.map((num) => num * 2);
console.log(doubled); // [2, 4, 6, 8]
```

### `forEach()` ফাংশন:

`forEach()` একটি array মেথড যা প্রতিটি উপাদানে একটি কলব্যাক ফাংশন প্রয়োগ করে কিন্তু কিছু রিটার্ন করে না।

```javascript
const numbers = [1, 2, 3, 4];
numbers.forEach((num) => console.log(num * 2));
// Output: 2, 4, 6, 8
```

### মূল পার্থক্য:

| বৈশিষ্ট্য   | `map()`                      | `forEach()`                          |
| ----------- | ---------------------------- | ------------------------------------ |
| রিটার্ন মান | নতুন array রিটার্ন করে       | `undefined` রিটার্ন করে              |
| চেইনিং      | চেইনিং সম্ভব                 | চেইনিং সম্ভব নয়                     |
| ব্যবহার     | উপাদান রূপান্তরের জন্য আদর্শ | শুধুমাত্র পার্শ্ব-প্রতিক্রিয়ার জন্য |
| উদাহরণ      | ডেটা রূপান্তর, ফিল্টারিং     | লগিং, অ্যাকশন সম্পাদন                |

**ব্যবহারিক পার্থক্য:**

```javascript
// map() ব্যবহার - রূপান্তর সহ
const prices = [10, 20, 30];
const withTax = prices.map((price) => price * 1.1); // নতুন array পাই
console.log(withTax); // [11, 22, 33]

// forEach() ব্যবহার - শুধুমাত্র পুনরাবৃত্তি
prices.forEach((price) => {
  console.log(`Price: $${price}`); // শুধু প্রিন্ট করি
});
```

---

## ৩. `==` এবং `===` এর মধ্যে পার্থক্য কি?

**উত্তর:**

### `==` (Loose Equality - ঢিলেঢালা সমতা):

- তুলনা করার আগে মান গুলি সমান ধরনে রূপান্তরিত হয়
- এটি Type Coercion করে

```javascript
console.log(5 == "5"); // true (সংখ্যা এবং স্ট্রিং সমান বলে বিবেচিত)
console.log(0 == false); // true
console.log(null == undefined); // true
```

### `===` (Strict Equality - কঠোর সমতা):

- মূল্য এবং ডেটা টাইপ উভয়ই সমান হতে হবে
- কোনো Type Coercion নেই

```javascript
console.log(5 === "5"); // false (টাইপ ভিন্ন)
console.log(0 === false); // false
console.log(null === undefined); // false
```

### সারণী তুলনা:

| তুলনা                 | `==` | `===` |
| --------------------- | ---- | ----- |
| `5 == "5"`            | true | false |
| `0 == false`          | true | false |
| `null == undefined`   | true | false |
| `1 === 1`             | true | true  |
| `"hello" === "hello"` | true | true  |

### সর্বোত্তম অনুশীলন:

সর্বদা `===` ব্যবহার করুন কারণ এটি আরও নির্ভরযোগ্য এবং অপ্রত্যাশিত ফলাফল এড়ায়।

```javascript
// ভুল - Type Coercion সমস্যা
if (userInput == 0) {
  // ঝুঁকিপূর্ণ
}

// সঠিক - নিরাপদ
if (userInput === 0) {
  // নিরাপদ
}
```

---

## ৪. API ডেটা ফেচ করার ক্ষেত্রে `async`/`await` এর গুরুত্ব কি?

**উত্তর:**

### `async` এবং `await` কি:

- `async` একটি ফাংশনকে asynchronous করে তোলে এবং এটি সর্বদা একটি Promise রিটার্ন করে
- `await` একটি Promise সমাধানের জন্য অপেক্ষা করে এবং ফলাফল রিটার্ন করে

### API ডেটা ফেচ করার উদাহরণ:

#### ঐতিহ্যবাহী উপায় (সমস্যাপূর্ণ):

```javascript
let data;
fetch("https://api.example.com/products")
  .then((response) => response.json())
  .then((result) => {
    data = result; // এখানে ডেটা সেট হয়েছে
  });

console.log(data); // undefined - কারণ fetch এখনও সম্পূর্ণ হয়নি
```

#### `async`/`await` সফাই:

```javascript
async function getProducts() {
  try {
    const response = await fetch("https://api.example.com/products");
    const data = await response.json();
    return data; // এখন আমরা সরাসরি ডেটা ব্যবহার করতে পারি
  } catch (error) {
    console.error("Error:", error);
  }
}

const products = await getProducts();
console.log(products); // সঠিক ডেটা পাওয়া যায়
```

### গুরুত্ব এবং সুবিধা:

1. **পাঠযোগ্যতা**: কোড সাধারণ synchronous থাকার মতো লাগে
2. **ত্রুটি পরিচালনা**: `try-catch` ব্যবহার করা সহজ
3. **ক্রম নিয়ন্ত্রণ**: মাল্টিপল API কল সহজে সংগঠিত করা যায়

```javascript
// একাধিক API কল
async function loadAllData() {
  try {
    const categories = await fetch("...categories").then((r) => r.json());
    const products = await fetch("...products").then((r) => r.json());
    const users = await fetch("...users").then((r) => r.json());

    return { categories, products, users };
  } catch (error) {
    console.error("Error loading data:", error);
  }
}
```

### স্থানীয় স্টোরেজ অনুশীলন:

SwiftCart প্রকল্পে আমরা `async`/`await` ব্যবহার করি সমস্ত API কলের জন্য সুবিধা এবং নির্ভরযোগ্যতার জন্য।

---

## ৫. জাভাস্ক্রিপ্টে স্কোপের ধারণা ব্যাখ্যা করুন (গ্লোবাল, ফাংশন, ব্লক)

**উত্তর:**

Scope নির্ধারণ করে যে একটি ভেরিয়েবল, ফাংশন বা অবজেক্ট কোথায় অ্যাক্সেসযোগ্য। জাভাস্ক্রিপ্টে তিনটি ধরনের স্কোপ আছে:

### ১. গ্লোবাল স্কোপ (Global Scope):

- সর্বোচ্চ স্তরে ঘোষিত ভেরিয়েবল
- সমগ্র প্রোগ্রামে অ্যাক্সেসযোগ্য

```javascript
let globalVar = "I am global"; // গ্লোবাল স্কোপ

function myFunction() {
  console.log(globalVar); // অ্যাক্সেসযোগ্য - "I am global"
}

myFunction();
console.log(globalVar); // অ্যাক্সেসযোগ্য - "I am global"
```

### ২. ফাংশন স্কোপ (Function Scope):

- ফাংশনের ভিতরে ঘোষিত ভেরিয়েবল
- শুধুমাত্র সেই ফাংশনের মধ্যে অ্যাক্সেসযোগ্য

```javascript
function myFunction() {
  let localVar = "I am local"; // ফাংশন স্কোপ
  console.log(localVar); // অ্যাক্সেসযোগ্য
}

myFunction();
console.log(localVar); // ReferenceError: localVar is not defined
```

### ৩. ব্লক স্কোপ (Block Scope):

- `{}` এর মধ্যে `let` বা `const` দিয়ে ঘোষিত ভেরিয়েবল
- শুধুমাত্র সেই ব্লকের মধ্যে অ্যাক্সেসযোগ্য

```javascript
// উদাহরণ 1: if ব্লক
if (true) {
  let blockVar = "I am in block"; // ব্লক স্কোপ
  const blockConst = "Also in block";
  console.log(blockVar); // অ্যাক্সেসযোগ্য
}

console.log(blockVar); // ReferenceError
console.log(blockConst); // ReferenceError

// উদাহরণ 2: for লুপ
for (let i = 0; i < 3; i++) {
  console.log(i); // 0, 1, 2 - অ্যাক্সেসযোগ্য
}

console.log(i); // ReferenceError: i is not defined

// কিন্তু var ব্যবহার করলে:
for (var j = 0; j < 3; j++) {
  console.log(j);
}

console.log(j); // 3 - অ্যাক্সেসযোগ্য (var এর জন্য কোনো ব্লক স্কোপ নেই)
```

### স্কোপ চেইন (Scope Chain):

ভেরিয়েবল খোঁজার প্রক্রিয়া অভ্যন্তরীণ থেকে বাহ্যিক স্কোপে:

```javascript
let globalVar = "global";

function outer() {
  let outerVar = "outer";

  function inner() {
    let innerVar = "inner";
    console.log(innerVar); // "inner" - স্থানীয় স্কোপ
    console.log(outerVar); // "outer" - বাহ্যিক ফাংশন স্কোপ
    console.log(globalVar); // "global" - গ্লোবাল স্কোপ
  }

  inner();
}

outer();
```

### সারণী: স্কোপের তুলনা:

| পার্থক্য         | গ্লোবাল               | ফাংশন                 | ব্লক           |
| ---------------- | --------------------- | --------------------- | -------------- |
| ঘোষণা স্থান      | প্রোগ্রাম শুরু        | ফাংশনের ভিতরে         | `{}` এর ভিতরে  |
| অ্যাক্সেস        | সর্বত্র               | ফাংশনের মধ্যে         | ব্লকের মধ্যে   |
| ভেরিয়েবল        | `var`, `let`, `const` | `var`, `let`, `const` | `let`, `const` |
| `var` পুনর্ঘোষণা | সম্ভব                 | সম্ভব                 | সম্ভব          |

### সর্বোত্তম অনুশীলন:

- গ্লোবাল ভেরিয়েবল এড়ান - নামের সংঘর্ষ সৃষ্টি করে
- সর্বদা `const` ব্যবহার করুন ডিফল্টে
- প্রয়োজন হলেই `let` ব্যবহার করুন
- কখনও `var` ব্যবহার করবেন না

---

## প্রকল্প কাঠামো

```
mission-restart-a6/
├── index.html          # হোম পেজ
├── products.html       # পণ্য পেজ
├── style.css          # সকল স্টাইলিং
├── script.js          # সকল কার্যকারিতা
├── README.md          # এই ফাইল
└── Assets/
    └── banner-image.png
```

## প্রযুক্তি স্ট্যাক

- **HTML5** - সিমান্টিক মার্কআপ
- **CSS3** - প্রতিক্রিয়াশীল ডিজাইন এবং অ্যানিমেশন
- **Vanilla JavaScript** - কোনো ফ্রেমওয়ার্ক নেই
- **Fake Store API** - পণ্য ডেটার জন্য

## বৈশিষ্ট্য বাস্তবায়ন

✅ রেসপন্সিভ নেভিগেশন বার
✅ হিরো ব্যানার বিভাগ
✅ কেন আমাদের বেছে নিন বিভাগ (4টি বৈশিষ্ট্য)
✅ ট্রেন্ডিং পণ্য (শীর্ষ 3 রেটেড)
✅动态 ক্যাটাগরি ফিল্টারিং
✅ পণ্য বিস্তারিত মডাল
✅ শপিং কার্ট কার্যকারিতা
✅ স্থানীয় সঞ্চয় প্রতিশ্রুতি
✅ সংবাদপত্র সাবস্ক্রিপশন ফর্ম
✅ পেশাদার নীচের অংশ

## কীভাবে চালাবেন

1. `index.html` খুলুন আপনার ব্রাউজারে
2. সমস্ত API ডেটা স্বয়ংক্রিয়ভাবে লোড হবে
3. ক্যাটাগরি ফিল্টার করুন এবং পণ্য ব্রাউজ করুন
4. কার্টে যোগ করুন এবং স্থানীয় সঞ্চয়ে সংরক্ষিত হবে

---

**স্থাপনা**: এই প্রকল্পটি যেকোনো স্ট্যাটিক হোস্টিং সেবায় স্থাপন করা যায়।

**লেখক**: SwiftCart টিম  
**তারিখ**: ফেব্রুয়ারি ২০২৫
